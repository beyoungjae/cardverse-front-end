name: Notify Slack

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, closed]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send message to Slack
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
          SLACK_MESSAGE: "🚀 *GitHub Update* \n🔹 Repository: ${{ github.repository }} \n🔹 Event: ${{ github.event_name }} \n🔹 Actor: ${{ github.actor }}"
